language: python

python:
  - 2.5
  - 2.6
  - 2.7
  - 3.2
  - pypy

matrix:
  allow_failures:
    - python: 3.2
    - python: pypy

script:
  - pip install -q --use-mirrors nose
  - python setup.py install
  - nosetests -w /tmp blessings.tests
